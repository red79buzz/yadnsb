# Server Configuration
PORT=3000
NODE_ENV=production

# Optional: Custom host binding (default: all interfaces)
# HOST=0.0.0.0

# Optional: Enable debug logging
# DEBUG=yadnsb:*

# Optional: Custom timeout for DNS queries (milliseconds)
# DNS_TIMEOUT=5000

# Optional: Maximum concurrent DNS queries
# MAX_CONCURRENT_QUERIES=10